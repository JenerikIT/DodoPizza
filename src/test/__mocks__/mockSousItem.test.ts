import { render, screen } from "@testing-library/react";
import baseComponent from "../../pages/CardMore";
describe("baseComponent component", () => {
  test("base test", () => {
    render(<baseComponent/>);
  });
});
